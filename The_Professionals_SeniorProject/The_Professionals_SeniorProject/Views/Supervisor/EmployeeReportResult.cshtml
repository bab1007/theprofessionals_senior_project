@using System.Linq

@model The_Professionals_SeniorProject.Models.Viewmodels.AccomplishmentViewModel

@{
    List<Accomplishment> WorkAccomplishments = new List<Accomplishment>();
    List<Accomplishment> CourseAccomplishments = new List<Accomplishment>();
    List<Accomplishment> ReadAccomplishments = new List<Accomplishment>();
    List<Accomplishment> MediaAccomplishments = new List<Accomplishment>();

    foreach (var item in (@Model.UserAccomplishments.Where(a => a.AchievementType == "Work")))
    {
        WorkAccomplishments.Add(item);

    }

    foreach (var item in (@Model.UserAccomplishments.Where(a => a.AchievementType == "Course")))
    {
        CourseAccomplishments.Add(item);

    }

    foreach (var item in (@Model.UserAccomplishments.Where(a => a.AchievementType == "Read")))
    {
        ReadAccomplishments.Add(item);

    }

    foreach (var item in (@Model.UserAccomplishments.Where(a => a.AchievementType == "Media")))
    {
        MediaAccomplishments.Add(item);

    }


}
<!-- ======================= Main Work Area ================================-->

<main role="main" id="resume" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
        <a class="text-secondary" asp-action="EmployeeReport">
            <h1 class="h2">Supervisor View</h1>
        </a>
    </div>

    <!-- ================== Internal Resume  ======================-->

    <h2> Internal Resume</h2><br>
    <div class="container border border-dark rounded">
        <h3 class="text-center mt-3 ">@ViewBag.UserName</h3><br>
        <div class="row">
            <!-- ============== Each Achievment Type will have their own int-resume div and replicate <li> with foreach loop =====-->
            <!-- ================== Check for Work Achievement Types  ======================-->

            @if (WorkAccomplishments.Count != 0)
            {
                <div class="int-Resume bg-info">
                    <h4 class="ml-4 pt-2 text-white">Work Experience</h4>
                    <ul class="list-group list-group-flush">

                        @foreach (var item in WorkAccomplishments)
                        {
                            <li class="list-group-item">
                                <div class="col-lg-4 float-left">
                                    <p><b>Project Name: </b> @item.Provider</p>
                                    <p><b>Job Title: </b> @item.JobTitle</p>
                                    <p><b>Dates: </b> @item.DateStarted.ToString("MM/dd/yyyy") - @item.DateCompleted.ToString("MM/dd/yyyy")</p>
                                </div>

                                <div class="col-lg-8 float-right">
                                    <p><b>Responsibilities:</b></p>
                                    <p>@item.Description</p>
                                </div>
                            </li>
                        }

                    </ul>
                </div>
            }
            <!-- ================== Check for Course Achievement Types  ======================-->

            @if (CourseAccomplishments.Count != 0)
            {
                <div class="int-Resume bg-info">
                    <h4 class="ml-4 pt-2 text-white">Courses & Training</h4>
                    <ul class="list-group list-group-flush">
                        @foreach (var item in CourseAccomplishments)
                        {
                            <li class="list-group-item">
                                <div class="col-lg-4 float-left">
                                    <p><b>School/Provider: </b> @item.Provider</p>
                                    <p><b>Activity Name: </b> @item.Activity</p>
                                    <p><b>Dates: </b> @item.DateStarted.ToString("MM/dd/yyyy") - @item.DateCompleted.ToString("MM/dd/yyyy")</p>
                                    <p><b>Instructor:</b> @item.ContactPerson</p>
                                </div>
                                <div class="col-lg-8 float-right">
                                    <p><b>Course Description:</b></p>
                                    <p>@item.Description</p>
                                </div>
                            </li>
                        }

                    </ul>
                </div>
            }




            <!-- ================== Check for Read Achievement Types  ======================-->

            @if (ReadAccomplishments.Count != 0)
            {
                <div class="int-Resume bg-info">
                    <h4 class="ml-4 pt-2 text-white">Relevant Reading</h4>
                    <ul class="list-group list-group-flush">
                        @foreach (var item in ReadAccomplishments)
                        {
                            <li class="list-group-item">
                                <div class="col-lg-4 float-left">
                                    <p><b>Author: </b> @item.Author</p>
                                    <p><b>Title: </b> @item.BookTitle</p>
                                    <p><b>Dates: </b> @item.DateStarted.ToString("MM/dd/yyyy") - @item.DateCompleted.ToString("MM/dd/yyyy")</p>
                                    <p><b>URL:</b> @item.URL</p>
                                </div>
                                <div class="col-lg-8 float-right">
                                    <p><b>Course Description:</b></p>
                                    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</p>
                                </div>
                            </li>

                        }

                    </ul>
                </div>
            }
            <!-- ================== Check For Media Achievement Types =======================-->

            @if (MediaAccomplishments.Count != 0)
            {
                <div class="int-Resume bg-info">
                    <h4 class="ml-4 pt-2 text-white">Media</h4>
                    <ul class="list-group list-group-flush">

                        @foreach (var item in MediaAccomplishments)
                        {
                            <li class="list-group-item">

                                <div class="col-lg-4 float-left">
                                    <p><b>Project Name: </b> @item.Provider</p>
                                    <p><b>Job Title: </b> @item.Provider</p>
                                    <p><b>Dates: </b> @item.DateStarted.ToString("MM/dd/yyyy") - @item.DateCompleted.ToString("MM/dd/yyyy")</p>
                                </div>

                                <div class="col-lg-8 mr-0 pr-0 float-right">
                                    <p><b>Responsibilities:</b></p>
                                    <p>@item.Description</p>
                                </div>

                            </li>
                        }

                    </ul>
                </div>
            }


        </div><br>
    </div>
    <br><br>
</main>